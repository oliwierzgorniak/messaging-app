import { redirect } from "react-router-dom";

const Home = () => {
  redirect("/signup");

  return (
    <div>
      <section></section>
      <main>
        <section></section>
      </main>
    </div>
  );
};

export default Home;
