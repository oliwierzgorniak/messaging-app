datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:example@localhost:5432/messaging-app?schema=public"
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  password  String 
}
